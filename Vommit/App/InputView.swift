import SwiftUI
import SwiftData

struct InputView: View {

    var body: some View {
        Text("Input VO₂ Max")
            .font(.largeTitle.bold())
        
        Text("Check minimum VO₂ Max \n to safely summit")
            .font(.callout)
            .multilineTextAlignment(.center)
        
        
        
        
    }

}

#Preview {
    ContentView()
}

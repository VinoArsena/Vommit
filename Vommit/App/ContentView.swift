import SwiftUI
import SwiftData

struct ContentView: View {

    var body: some View {
        Text("VOMMIT")
            .font(.largeTitle.bold())
        
        Text("Check minimum VO₂ Max \n to safely summit")
            .font(.callout)
            .multilineTextAlignment(.center)
        
        
        
        
    }

}

#Preview {
    ContentView()
}

import SwiftUI

struct TrainingView: View {
    
    var body: some View {
        Text("Text")
    }
}

#Preview {
    TrainingView()
        .preferredColorScheme(.dark)
        .background(Color("Background").ignoresSafeArea())
}

